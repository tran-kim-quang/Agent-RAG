chunking:
  chunk_size: 1000
  chunk_overlap: 200
  semantic_window_size: 3000
  semantic_window_overlap: 200

retriever:
  top_k: 5
  graph_max_hops: 2 # Number of hops for graph retrieval
  rrf_k: 60 # Smoothing parameter for Reciprocal Rank Fusion (RRF)

generation:
  max_tokens: 2048
  temperature: 0.0
